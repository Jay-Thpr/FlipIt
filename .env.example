# FastAPI
APP_HOST=0.0.0.0
APP_PORT=8000
APP_BASE_URL=http://localhost:8000

# Agent networking
AGENT_HOST=127.0.0.1
AGENT_EXECUTION_MODE=local_functions

# Internal security
INTERNAL_API_TOKEN=dev-internal-token

# Fetch.ai (optional; see FetchAI-Status.md)
FETCH_ENABLED=false
AGENTVERSE_API_KEY=

# Agent execution
AGENT_TIMEOUT_SECONDS=30
BUY_AGENT_MAX_RETRIES=1

# Browser Use / Gemini
GOOGLE_API_KEY=
ANONYMIZED_TELEMETRY=false
BROWSER_USE_GEMINI_MODEL=gemini-2.0-flash
BROWSER_USE_PROFILE_ROOT=profiles
BROWSER_USE_MAX_STEPS=15
BROWSER_USE_FORCE_FALLBACK=false

# ── eBay Browse API ────────────────────────────────────────────────────────
# Required for eBay active listing search via official Browse API (BUY pipeline)
# Register at https://developer.ebay.com/my/keys
EBAY_APP_ID=your_ebay_app_id_here
EBAY_CERT_ID=your_ebay_cert_id_here
